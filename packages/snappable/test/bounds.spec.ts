import { Snappable } from "src/Snappable";
import { } from "overlap-area";
describe("aa", () => {
    it("aa", () => {
        const points1 = [
            [0, 0],
        ]
        getOverlapPoints
    });
});
